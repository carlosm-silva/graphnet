# PACE-compatible pinned application dependencies. This include is resolved
# relative to this file by pip.
-r docs/graphnet_env/pace-pip-requirements.txt

# GraphNeT itself must be installed editable from the enclosing checkout at the
# commit pinned in docs/graphnet_env/README.md:
#   python -m pip install --no-build-isolation --no-deps --editable .
