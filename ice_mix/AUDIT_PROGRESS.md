# IceMix documentation audit progress

## Status

- Current phase: Audit complete; all four phases and final consistency checks are finished.
- Cold-read embargo honored: no `.py`, `.sbatch`, configuration, `MEMORY.md`, `TODO.md`, or `QUESTIONS.md` was inspected before these notes were written.
- Documentation read in its prescribed order: root `README.md`; `docs/index.md`; glossary, GraphNeT boundary, configuration, PACE, job catalog, and handoff status; tutorials 1–6 (including the suggested smoke example between tutorials 2 and 3); then all folder README files.
- Phase 1 executable check: from `docs/_local_sample`, literal `python verify_sample.py` failed because `python` is absent from the current shell; `python3 verify_sample.py` passed all six reported integrity groups. No training, GraphNeT import, GPU, PACE, or production-data check was attempted.
- Coverage counts from `/tmp/ice_mix_ast_inventory.py`: 36 modules (36 documented), 23 classes (22 documented), 72 methods (54 documented), 142 top-level functions, and 119 public top-level functions (104 documented). The 15 undocumented public functions are all under `docs/_local_sample`; 17 undocumented methods are in live `src`, with one additional undocumented test-fixture method/class.
- Submission inventory from `/tmp/ice_mix_launcher_inventory.py`: 44 root helpers (42 `.sbatch`, 2 `.sh`) were mechanically read. Of them, 35 contain the former author’s email, 43 his project path, 20 his scratch path, 7 another user’s home path, 11 hard-code `--nproc_per_node=8`, and 33 have an unguarded absolute-storage `cd`. `bash -n ice_mix/*.sbatch ice_mix/*.sh` passed. The documentation globally flags the account/path strings, so their mere presence is not a finding; the unflagged personal CSV in `plot_master.py` is different and is confirmed as a blocker.
- Process-integrity note: a broad Phase 2 `rg` command intended to find environment leakage inadvertently printed matching snippets from `MEMORY.md` after the Phase 1 embargo had ended but before Phase 4. No `TODO.md` or `QUESTIONS.md` content was read, and the leaked snippets are not being used as audit evidence before the full Phase 4 ledger read. This cannot be undone and is recorded here rather than concealed.

## Phase 1 raw notes (unresolved, in encounter order)

These are intentionally untriaged. Phase 2 must either confirm each gap or explicitly discard it.

1. `README.md` lines 5–7 immediately makes the departed original author on Slack the fallback for unclear intent. I had to assume that contact still exists and is responsive six months later; no repository-only escalation path is given.
2. `README.md` lines 16–20 says every non-obvious hand-written claim is tier-marked. I could not tell whether unmarked tables, tutorial instructions, and folder README claims are intended to inherit a later blanket marker or violate that policy.
3. `docs/index.md` lines 11–17 names pulse features only generically (position, time, charge, relative DOM efficiency, “related detector features”). I still did not know the ordered model inputs, their units, normalization, or why the seventh named feature later turns out to be unused.
4. `docs/index.md` lines 19–25 gives the loss and `alpha=0.026` without explaining its derivation, the loss reductions/units, or how that weighting should be interpreted. I would have to ask the author or read source before changing targets or judging loss magnitudes.
5. `docs/index.md` lines 29–45 uses `IceCube86`, `IceMixNodes`, “Fourier features,” “spacetime-relative transformer,” and “GraphNeT joint task” before the linked glossary/boundary material defines only some of them. The flow is orienting, but not sufficient to reproduce shapes or transformations.
6. `docs/glossary.md` line 21 says oneweight is propagated but its normalization/use is undefined in IceMix. That leaves no documented route to decide whether physics plots are weighted correctly.
7. `docs/glossary.md` line 27 says rotation is consistent, but does not state the angle distribution, coordinate handedness, azimuth convention, or periodic wrapping.
8. `docs/graphnet-boundary.md` lines 9 and 17 label GraphNeT normalization and vertex distance as conventions/metres, while line 21 says physical units still require provenance confirmation. I could not tell what is known from code versus merely inferred from an author-supplied manifest.
9. `docs/graphnet-boundary.md` lines 35–51 exposes a hard stop: the declared `graphnet>=1.0.0` does not guarantee two required custom interfaces, and the recovery instruction is to find an exact environment/commit in the departed author’s PACE files. No checked-in version, commit, lock/export, or reproducible fallback is supplied.
10. The import check in `docs/graphnet-boundary.md` lines 44–46 proves only that two names exist, not that the whole checkout/API is version-compatible.
11. `docs/configuration.md` lines 24–30 lists architecture settings but does not describe tensor shapes through attention, the meaning of the spacetime alternatives, or constraints beyond `hidden_dim % head_size == 0`. A model-changing student is sent toward source.
12. `docs/configuration.md` lines 34–39 does not state dropout defaults, rotation distribution/convention, whether validation is augmented, or exact token-drop sampling semantics beyond two probabilities.
13. `docs/configuration.md` line 43 says feature indices 0–5 are used but never gives the ordered index-to-feature mapping. This is a silent feature-contract guess with physics consequences.
14. `docs/configuration.md` lines 61–63 names encoder multipliers/clips and warns that their derivation is unverified, but does not state their values, formulas, affected coordinates/units, or expected ranges. I would have to open config/source to understand the model’s numerical representation.
15. `docs/configuration.md` lines 81–83 points to a stale `config_parameters_guide.md` without locating it and tells the reader to treat YAML/source as authoritative. The docs knowingly delegate current semantics to config and source.
16. `docs/pace-phoenix.md` lines 7–12 makes onboarding depend on both the original author and an undefined person (“Jiyuan”) on Slack. No role, full identity, group channel, or alternative owner is given.
17. `docs/pace-phoenix.md` lines 31–37 says inherited account/path strings are an intentional map and may remain if authorized. Before reading scripts I could not tell which strings are shared group resources versus personal ownership, so safe replacement still requires external knowledge.
18. `docs/pace-phoenix.md` lines 39–61 repeats the environment blocker: an old module plus an unexported `graphnet` env is canonical; requirements are known to conflict; a local ad hoc compatibility set is “reference only.” A student cannot recreate a supported environment from the repository.
19. `docs/pace-phoenix.md` lines 74–92 tells the student to copy/edit a launcher and replace broad categories of values, but supplies no validated successor launcher template. The first production action therefore begins with modifying a script known to contain stale behavior.
20. `docs/pace-phoenix.md` lines 98–104 assumes the log parent/path pattern is known, while the job catalog later warns Slurm opens `-o` before in-job `mkdir`. I had to infer that the successor must pre-create the exact parent directory.
21. `docs/pace-phoenix.md` lines 110–121 gives “finite loss” and checkpoint presence as health criteria but no expected scale/range or first-epoch sanity bounds. The one-epoch/hour report is author-reported, not reproducible here.
22. `docs/pace-phoenix.md` line 123 warns of Simpson’s paradox but does not define the exact topology classification, filtering, weighting, aggregation, or metric statistic used by the supplied plots. “Stratify” alone does not prevent a quietly wrong comparison.
23. `docs/job-scripts.md` lines 7–24 confirms that checked-in launchers retain personal email, personal project/scratch paths, another user’s home path, node names, and checkpoints. Although flagged globally, there is no machine-checkable preflight or clean template; missing one substitution can submit under inherited identity/path assumptions.
24. `docs/job-scripts.md` lines 51–69 documents several known unsafe/stale constructs (`cd` without failure handling, insufficient `/tmp` fallback, warnings on missing databases, hard-coded GPU counts). I could not tell which recommended launcher contains which repeated defect without opening each script.
25. `docs/job-scripts.md` lines 71–87 calls a launcher family “recommended” while every listed base launcher has a material defect: incomplete two-flavor staging or broken Hydra-path parsing and hard-coded eight workers. There is no checked-in recommended launcher described as ready to submit.
26. `docs/job-scripts.md` lines 77–81 says the production launcher’s staging “may fail” after interpolation. It does not give an observable pre-submit test or exact expected staged file list/log, leaving the student to debug on an allocation.
27. `docs/job-scripts.md` lines 136–147 says the shared helper warns rather than fails for missing databases and Python may fall back to project storage. I could not tell whether this silently creates mixed local/project I/O or whether three-flavor absence is eventually fatal.
28. `docs/handoff-status.md` lines 9–25 lists environment, units/provenance, node selection, DDP, staging, resume, runtime, memory, convergence, and numerical-conditioning value as still unverified. These include prerequisites for safe production and physics interpretation, not merely optional refinements.
29. `docs/handoff-status.md` lines 37–48 claims independent checks and strict checkpoint loading but does not identify the independent command/operator or provide its output. I could reproduce only the dependency-light fixture check during the cold read.
30. `docs/handoff-status.md` lines 52–62 describes a disposable environment where no GPU was visible, despite this audit workstation’s stated RTX 3070 Ti baseline. I could not tell whether the earlier claim reflects a container, a driver failure, or an environment that no longer exists.
31. Tutorial 1 prerequisites (`docs/tutorials/01_day_one_and_pace.md` lines 3–7) include the departed author’s Slack contact and allocation sponsorship. Its 1–3 hour estimate excludes not only account approval but also the undocumented environment/data handoff it requires.
32. Tutorial 1 lines 51–69 has no recovery path if the author’s environment export cannot be found; “stop” and contact the author is exactly where I would give up on repository documentation.
33. Tutorial 1 lines 71–80 labels the config-resolution command VERIFIED-LOCAL but its successful run depended on an unstated staged `DATA_ROOT` and compatible environment. The docs do not provide a locally reproducible equivalent at that point.
34. Tutorial 2 lines 13–32 assumes three oddly named authoritative SQLite files already exist. If naming differs it recommends an override or group-approved symlink policy but gives neither a concrete override nor ownership contact beyond Slack.
35. Tutorial 2 lines 45–58 makes units/provenance a mandatory external confirmation. “Jiyuan” remains undefined, and no durable data manifest/version identifier is supplied.
36. Tutorial 2 lines 60–64 says only indices 0–5 reach Fourier encoding but still does not map those indices to features or state normalization formulas.
37. Tutorial 2 lines 68–82 calls random splitting the working default but never discusses event identity overlap/duplication across the three databases, leakage controls, stratification, or why an 80/10/10 per-file random split is scientifically acceptable. The frozen production CSV selections are absent/incomplete and must be obtained externally.
38. The single-GPU example (`docs/examples/README.md` lines 3–16) requires the same missing compatible GraphNeT environment and three correctly named databases, so it is not a bootstrap path. It has never completed end to end even on the documenting workstation.
39. The documented literal dependency-light check in `docs/_local_sample/README.md` lines 3–5 failed in this shell because `python` is absent; `python3` passed. This is recoverable friction, but the environment prerequisite is implicit.
40. `docs/_local_sample/README.md` lines 43–53 first changes directory to `ice_mix/docs/_local_sample`, then gives `PYTHONPATH=ice_mix python ice_mix/docs/_local_sample/verify_graphnet.py`. If followed in the same shell, both relative paths point below the sample directory and appear wrong; the docs do not say to return to the repository root.
41. `docs/_local_sample/README.md` lines 55–58 says to use `sample_data.yaml` for an application smoke test but supplies no command or Hydra override. I would have to inspect configs/source to discover how.
42. Tutorial 3 lines 24–55 recommends `run_augmented.sbatch` after telling the student to repair all author values and ensure three-file staging. The job catalog simultaneously says this launcher has broken/stale staging and hard-coded worker count. The tutorial does not present the required repair, so submission is not actually step-by-step.
43. Tutorial 3 lines 68–90 assumes checkpoint filenames/output layout and newest-run resume selection are safe. No instruction proves that `last.ckpt` is complete/compatible before resume, and automatic newest-prefix selection can choose the wrong scientific run; the only guard is reading a log after submission begins.
44. Tutorial 4 lines 13–28 says dry-run discovers and selects checkpoints but does not show expected output or how skipped/multiple runs are reported. I would need to run or read source to know what “confirm manually” entails.
45. Tutorial 4 lines 30–44 again requires repairing a known stale inference launcher. There is no production-ready three-flavor prediction launcher.
46. Tutorial 4 lines 48–70 lists result columns but does not document units for every copied/predicted field in the tutorial itself, row identity uniqueness, weighting semantics, selection metadata, or how the output records the checkpoint/code/data version. A CSV can therefore look valid while being scientifically irreproducible.
47. Tutorial 4 lines 84–104 explicitly says not to interpret weighted/unweighted distributions without reading the plotting function. The documentation exists to avoid source archaeology; here it requires it and does not state what the supplied plotting commands actually do.
48. Tutorial 5 lines 21–34 calls the 1% token-removal run a wiring check but does not state the sampling seed/default or whether the same event identities/drop masks are held across percentages.
49. Tutorial 5 lines 45–65 provides a checkerboard command with no `--dry-run`, no output expectation, and no stated cell size, origin, parity rule, coordinate system, or units. It explicitly tells the student to “Read `_checkerboard_forward`,” a highest-value cold-read failure: the tutorial delegates the core scientific perturbation definition to source.
50. Tutorial 5 lines 67–76 demands consistent weighting/filtering and coordinate conventions without documenting either the scripts’ actual defaults or a reproducible verification command. A student can satisfy the prose in name while comparing different samples.
51. Tutorial 6 lines 13–21 names change boundaries but still requires an undocumented understanding of tensor layouts; that understanding is itself a prerequisite on line 5 rather than something the handoff teaches.
52. Tutorial 6 lines 23–32 gives three tests but does not state expected test counts/output, environment, or whether these are the complete relevant tests. `compileall` is not a behavioral check.
53. `scripts/README.md` says index creation mutates a controlled copy but does not define when indexes are required, expected performance effect, or a verification/rollback procedure. Its workflow status is left to the departed author.
54. `src/README.md` line 10 says `src/` relies on `ice_mix` being on `sys.path`, but tutorials do not explain the supported working directory/import setup beyond one shell guard.
55. `outputs/README.md` says checked-in outputs are stale provenance and missing checkpoints/full outputs/logs. It does not identify which artifacts are trustworthy for which historical run, so a student cannot safely use the folder as either an example or a canonical input.
56. `docs/_local_sample/README.md` lines 9–24 says fixtures preserve real simulated values and exact run artifacts but gives no durable source-data checksum/version or extraction date beyond run naming. I could verify internal integrity, not scientific provenance.
57. Across the cold read, many substantial behavior statements in the glossary, folder README files, tutorial steps, and job tables have no marker adjacent to them despite the root promise that every non-obvious statement is marked. Phase 3 must count and classify this rather than assuming blanket markers.

## Files/symbols audited

- Documentation-only files read: all Markdown files under `ice_mix/docs/` and every `README.md` under `ice_mix/`.
- Documentation executable read as text: `docs/examples/run_single_gpu_smoke.sh` only (it is part of the explicitly linked documentation example, not a submission helper).
- Phase 2 source/config files audited so far: all current Hydra YAML; `requirements.txt`; `config_parameters_guide.md`; `train.py`; `src/utils.py`; `src/metrics_logging.py`; every file under `src/models/`; `predict.py`; `resilience_test.py`; `checkerboard_test.py`; `plot_utils.py`; `plot_run.py`; `plot_resilience_test.py`; `plot_checkerboard_test.py`; `generate_plots.py`; `plot_master.py`; `plot_reference.py`; `generate_nutau_comparison_plots.py`; `generate_fine_tune_comparison_plots.py`; and `evaluate_rotation_checkpoint_averaging.py`.
- Every root `.sbatch` and `.sh` file has been mechanically read and its directives, resources, staging basenames, rank selection, absolute paths, and invoked command inventoried. Representative recommended, resume, fine-tune, prediction, robustness, EMA, and historical-probe files were then checked line by line against `docs/job-scripts.md`.
- All 36 Python modules have now been inspected or mechanically parsed, including `verify_config.py`, `reproduce_token_drops.py`, `scripts/add_indices.py`, all seven root test/diagnostic modules, and all three `docs/_local_sample` Python helpers. No Python module remains for Phase 2 coverage.
- Boundary-only GraphNeT sources inspected to verify inherited contracts: `src/graphnet/data/constants.py`, `models/components/embedding.py`, `models/detector/icecube.py`, and the custom data module implementation.
- Confirmed Phase 1 gaps/findings so far: observations 3–4, 6–15, 18–19, 25–28, 32, 36–38, 42, 46–51 are genuine in whole or material part. Final grouping is being kept high-signal in `AUDIT.md`.
- Explicitly discarded/qualified Phase 1 observations so far: observation 7’s missing rotation distribution/wrapping is supplied by the `RandomRotationCallback` docstring, but it remains absent from the student-facing narrative and the seed exception is genuine; observations 17 and 23 are not findings merely because inherited strings exist, because `docs/job-scripts.md` explicitly flags all of those categories; observation 37’s concern about overlap across flavor files is not evidenced and is discarded, while the missing split-provenance discussion remains; observation 43’s checkpoint completeness concern is not yet evidenced and is deferred rather than treated as a finding.
- Dead-code/reachability result: `src/trainer.py` is not imported or called outside itself, matching the documentation’s abandoned status. `DynEdgeConv`, `EdgeConvTito`, and `DynTrans` are likewise unreferenced outside `src/models/layers.py`; in contrast, `Block_rel` and `Attention_rel` are imported by the live transformer. No suspected-dead file documented as historical was found to be load-bearing.

## Mechanical coverage exceptions

- Undocumented public fixture functions (15): `build_samples.py` (`make_standard_database`, `write_selection`, `main`); `verify_graphnet.py` (`graph_definition`, `dataset`, `build_model`, `main`); and `verify_sample.py` (`event_ids`, six `check_*` functions, `main`).
- Undocumented live methods (17): `JointLossWithMetrics.__init__`, `_validate_joint_shapes`, `_forward`; `PerEventStats.__init__`; `NonFiniteLossCallback.__init__`; `PhysicsMetricsCallback.__init__`, `_phase_metrics`, `_batch_size`, `_ensure_metrics_on_device`, `_update`, `_flush`; `EMAStandardModel.__init__`; `IceMix._token_drop_generator`; `TokenDropSeedCallback.__init__`, `_batch_seed`; and `RandomRotationCallback.__init__`, `_apply_rotation`.
- Undocumented test fixture: `_FineTuneFixture` and its `__init__` in `test_lbfgs_model.py`.
- Vacuous representative docstrings: `IceMix` — “IceMix model (formerly DeepIce)”; `Block` — “Transformer block”; `Block_rel` — “Implementation of BEiTv2 Block”; `DynEdgeConv` — “Dynamical edge convolution layer.” Constructor docs partly compensate for some layer classes, but `IceMix.__init__` omits all nine numeric controls from `pos_time_multiplier` through `n_freq`.

## Phase 3 verification-integrity results

- Marker inventory across the root/folder READMEs and `docs/`: 47 `VERIFIED-STATIC`, 20 `VERIFIED-LOCAL`, 28 `UNVERIFIED-CLUSTER`, and 22 `author-confirmed` occurrences (including the marker-policy definitions themselves).
- Re-ran `python3 verify_sample.py`: all six groups passed. The detailed fixture assertions cover 30 events per flavor; specifically, the 50-row prediction sample is entirely finite and its largest direction-norm deviation is `8.46e-08`.
- The staged metrics CSV contains 125,922 sparse logger rows, 31 populated `train_loss` summaries and 31 populated `val_loss` summaries, with no non-finite populated metric value. This supports the documentation’s “31 train/validation epoch summaries” wording.
- The staged checkpoint is a valid 353,310,946-byte PyTorch zip archive with `data.pkl`, version metadata, and 968 tensor-storage members. Without PyTorch it was not possible to deserialize and independently enumerate Lightning dictionary keys, but the archive is substantive rather than a fabricated filename.
- The staged application-inference output contains nine rows and 19 fields; the separate reduced prediction sample contains 50 rows. The successful 31-million-character report contains the claimed eight-process, best-weight-restoration, and exit-code-zero landmarks. The failed report contains the claimed SQLite open error and exit-code-one landmarks.
- Reproduced the documented plotting failure using the 50-row sample and the existing `scientific-general` pandas/matplotlib environment: all and track figures were reached, then the seven-event cascade path raised `ValueError: Data has no positive values, and therefore cannot be log-scaled` during `tight_layout`.
- None of the five existing conda environments contains PyTorch, Hydra, OmegaConf, GraphNeT, PyG, or Lightning. Therefore the historical GraphNeT strict load/CPU forward, 21 test results, reduced model construction, full nine-event `predict.py` run, and Hydra composition could not be rerun. The local artifacts and exact version narrative are plausible and mutually consistent; no evidence of fabricated output was found. This inability is part of the environment blocker, not a separate fabrication finding.
- Current `nvidia-smi` also fails to communicate with the driver, consistent with the documented no-visible-GPU limitation despite the workstation hardware baseline.
- Marker integrity failed in two material cases already reported: Tutorial 5 blankets the perturbation algorithms as `VERIFIED-STATIC` even though its checkerboard description contradicts the code (BLOCKER 1), and an unmarked sample-README statement claims existing postprocessing uses `oneweight` although every supplied statistic is unweighted (MAJOR 10).
- The prescribed GraphNeT verification command is not runnable as sequenced: after the documented `cd ice_mix/docs/_local_sample`, its next relative path expands below that directory and fails before import. This is MINOR 13.

## Phase 4 ledger-honesty results

- `MEMORY.md`, `TODO.md`, and `QUESTIONS.md` were deliberately read only after Phases 1–3. They do not provide an honest resume point.
- Version-control audit at `4394131647b4a581e7d4923361b2814ab9e03ff5`: all handoff READMEs, `docs/`, and the three ledgers are untracked. The source/docstring work is modified but uncommitted. The large `_local_sample` directory is explicitly ignored by both root `.gitignore` line 150 and `docs/.gitignore` line 1, and no file under it is tracked. This is BLOCKER 5.
- `TODO.md` claims the final consistency pass complete and lists no Blocked or Deferred-data work, while `docs/handoff-status.md` still lists seven unresolved cluster/environment/data prerequisites. `QUESTIONS.md` marks the PACE operational, data provenance/units, and exact environment questions answered even though each “answer” defers to Jiyuan or the former author’s PACE files. `MEMORY.md` itself repeats them under Open uncertainties. This is MAJOR 11.
- `MEMORY.md` contradicts audited code in two load-bearing summaries: line 36 calls checkerboard removal alternating spatial cells, and lines 18–21 say the base launcher stages all three databases and sizes ranks from healthy GPUs. These contradictions are incorporated into findings 1 and 8 rather than duplicated.
- Ledger claims that held up: `src/trainer.py` and the named historical probes/launchers are genuinely unreachable/historical; the GraphNeT inheritance boundary summary is broadly accurate; the fixture/run evidence is internally consistent; and the stated local CUDA/NVML limitation matches the current workstation.

## Phase 1 disposition

- Confirmed and consolidated into final findings: observations 1–4, 6, 9–16, 18–20, 22, 24–28, 31–42, 45–52, 54, and 56–57. Repeated symptoms were intentionally grouped rather than emitted as separate findings.
- Observation 7 was qualified: the callback docstring supplies the rotation distribution/convention, so that part was a cold-read sequencing gap; the unrecorded-seed behavior remains genuine and is finding 9.
- Observation 8 was qualified: the boundary page honestly caveats physical-unit provenance, but it does not supply the inherited transforms/equations; the remaining contract gap is finding 6.
- Observations 17 and 23 were discarded as account-string findings because the job catalog explicitly flags those categories. The unflagged Python reference path is independently evidenced in finding 4.
- Observation 21 was not promoted: the docs explicitly mark runtime/loss expectations unverified, and this environment provides no evidence for a correct numerical range.
- Observation 29 was discarded: the staged artifacts and verifier substantiate the claimed independent read-only checks, even though the stronger PyTorch checks are no longer rerunnable here.
- Observation 30 was discarded: current `nvidia-smi` reproduces the documented driver/NVML failure, so the laptop/GPU statement is not contradictory.
- Observation 37 was split: cross-flavor event overlap was speculative and discarded; missing stored split provenance was confirmed in findings 2 and 7.
- Observation 43 was discarded: no evidence showed `last.ckpt` to be incomplete; automatic newest-run selection remains a documented risk but is subsumed by the explicit run-selection defects in finding 7.
- Observations 44 and 53 are real low-level usability gaps but were kept out of the final findings because the student can inspect logged dry-run selection or the index utility within minutes and higher-impact patterns already cover source archaeology.
- Observation 55 was discarded: `outputs/README.md` explicitly says those artifacts are stale provenance rather than canonical inputs, so refusing to assign them trust is correct.
- Observation 5 was discarded after the glossary/boundary pages supplied the delayed definitions; the exact feature/numeric contracts that remained missing are separately captured in finding 6.

## Remaining work

- None. All four phases and final consistency checks are complete.
